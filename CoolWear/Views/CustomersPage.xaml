<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CoolWear.Views.CustomersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoolWear.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:CoolWear.Models"
	mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0" Orientation="Horizontal" Padding="10" Spacing="8">
			<TextBox Width="300" PlaceholderText="Search customers..." Text="{x:Bind ViewModel.SearchTerm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
			<Button Content="Add Customer" Command="{x:Bind ViewModel.AddCustomerCommand}"/>
		</StackPanel>
		<ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.FilteredCustomers, Mode=OneWay}">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="models:Customer">
					<StackPanel Orientation="Horizontal">
						<TextBlock Text="{x:Bind CustomerName}" Width="200"/>
						<TextBlock Text="{x:Bind Email}" Width="200"/>
						<Button Content="Edit" Command="{Binding DataContext.EditCustomerCommand, ElementName=pageRoot}" CommandParameter="{x:Bind}"/>
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</Grid>
</Page>
