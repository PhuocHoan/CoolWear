<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CoolWear.Views.DashboardWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoolWear.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CoolWear">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- App Title Bar -->
		<local:CustomTitleBar x:Name="AppTitleBar" Grid.Row="0"/>

		<!-- Navigation View -->
		<NavigationView 
            x:Name="navigation" 
            Grid.Row="1"
            IsBackButtonVisible="Collapsed" 
            ItemInvoked="Navigation_ItemInvoked" 
            SelectionChanged="Navigation_SelectionChanged" 
            PaneDisplayMode="Top">

			<NavigationView.MenuItems>
				<NavigationViewItem Icon="Refresh" Content="Loại sản phẩm" Tag="CategoriesPage"/>
				<NavigationViewItem Icon="Refresh" Content="Sản phẩm" Tag="ProductsPage"/>
				<NavigationViewItem Icon="Refresh" Content="Khách hàng" Tag="CustomersPage"/>
				<NavigationViewItem Icon="Refresh" Content="Đơn hàng" Tag="OrdersPage"/>
				<NavigationViewItem Icon="Refresh" Content="Báo cáo" Tag="ReportPage"/>
			</NavigationView.MenuItems>

			<NavigationView.FooterMenuItems>
				<NavigationViewItem Content="Tài khoản" Tag="SamplePage4" Icon="Contact" />
			</NavigationView.FooterMenuItems>

			<Frame x:Name="container"/>
		</NavigationView>

		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup>
				<VisualState>
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="{x:Bind navigation.CompactModeThresholdWidth}" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="navigation.PaneDisplayMode" Value="Top" />
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>
	</Grid>
</Window>
