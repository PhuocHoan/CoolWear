<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CoolWear.Views.DashboardWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoolWear.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="DashboardWindow">

	<Grid>
		<!--<VisualStateManager.VisualStateGroups>
        <VisualStateGroup>
            <VisualState>
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="{x:Bind navigation.CompactModeThresholdWidth}" />
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="navigation.PaneDisplayMode" Value="Top" />
                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateManager.VisualStateGroups >-->

		<NavigationView x:Name="navigation" IsBackButtonVisible="Collapsed" ItemInvoked="navigation_ItemInvoked" SelectionChanged="navigation_SelectionChanged" PaneDisplayMode="Top">
			<NavigationView.Header>
				<StackPanel Orientation="Horizontal">
					<Image Source="/Assets/logo.png" Width="40" Height="40"/>
					<TextBlock Text="CoolWear" VerticalAlignment="Center" Margin="10,0,0,0"/>
				</StackPanel>
			</NavigationView.Header>
			<NavigationView.MenuItems>
				<NavigationViewItem Icon="Shop" Content="Dữ liệu cơ sở" IsExpanded="True">
					<NavigationViewItem.MenuItems>
						<NavigationViewItem Icon="Refresh" Content="Loại hàng" Tag="CategoriesPage"/>
						<NavigationViewItem Icon="Refresh" Content="Kho hàng" Tag="ProductsPage"/>
						<NavigationViewItem Icon="Refresh" Content="Khách hàng" Tag="CustomersPage"/>
					</NavigationViewItem.MenuItems>
				</NavigationViewItem>
				<NavigationViewItem Content="Dữ liệu theo thời gian" IsExpanded="True">
					<NavigationViewItem.Icon>
						<ImageIcon Source="/Assets/shops.png"/>
					</NavigationViewItem.Icon>
					<NavigationViewItem.MenuItems>
						<NavigationViewItem Icon="Refresh" Content="Đơn hàng" Tag="OrdersPage"/>
					</NavigationViewItem.MenuItems>
				</NavigationViewItem>
			</NavigationView.MenuItems>
				<NavigationView.FooterMenuItems>
				<NavigationViewItem Content="Tài khoản" Tag="SamplePage4" Icon="Contact" />
			</NavigationView.FooterMenuItems>
			<Frame x:Name="container"/>
		</NavigationView>
	</Grid>
</Window>
