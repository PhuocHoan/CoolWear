<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CoolWear.Views.AccountPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoolWear.Views"
    xmlns:viewModels="using:CoolWear.ViewModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <StackPanel>
            <!-- Tên chủ cửa hàng -->
            <TextBlock Text="Tên chủ cửa hàng:" FontSize="20" FontWeight="Bold"/>
            <Border BorderBrush="Black" BorderThickness="1" Margin="0,10,0,10">
                <TextBlock Text="{x:Bind ViewModel.OwnerName, Mode=OneWay}" FontSize="16" Padding="5"/>
            </Border>

            <!-- Email -->
            <TextBlock Text="Email:" FontSize="20" FontWeight="Bold"/>
            <Border BorderBrush="Black" BorderThickness="1" Margin="0,10,0,10">
                <TextBlock Text="{x:Bind ViewModel.Email, Mode=OneWay}" FontSize="16" Padding="5"/>
            </Border>

            <!-- Số điện thoại -->
            <TextBlock Text="Số điện thoại:" FontSize="20" FontWeight="Bold"/>
            <Border BorderBrush="Black" BorderThickness="1" Margin="0,10,0,10">
                <TextBlock Text="{x:Bind ViewModel.Phone, Mode=OneWay}" FontSize="16" Padding="5"/>
            </Border>

            <!-- Địa chỉ -->
            <TextBlock Text="Địa chỉ:" FontSize="20" FontWeight="Bold"/>
            <Border BorderBrush="Black" BorderThickness="1" Margin="0,10,0,10">
                <TextBlock Text="{x:Bind ViewModel.Address, Mode=OneWay}" FontSize="16" Padding="5"/>
            </Border>
        </StackPanel>
        <StackPanel>
            <!-- Tên chủ cửa hàng -->
            <TextBlock Text="Tên chủ cửa hàng:" FontSize="20" FontWeight="Bold"/>
            <Border BorderBrush="Black" BorderThickness="1" Margin="0,10,0,10">
                <TextBlock Text="{x:Bind ViewModel.OwnerName, Mode=OneWay}" FontSize="16" Padding="5"/>
            </Border>

            <!-- Email -->
            <TextBlock Text="Email:" FontSize="20" FontWeight="Bold"/>
            <Border BorderBrush="Black" BorderThickness="1" Margin="0,10,0,10">
                <TextBlock Text="{x:Bind ViewModel.Email, Mode=OneWay}" FontSize="16" Padding="5"/>
            </Border>

            <!-- Số điện thoại -->
            <TextBlock Text="Số điện thoại:" FontSize="20" FontWeight="Bold"/>
            <Border BorderBrush="Black" BorderThickness="1" Margin="0,10,0,10">
                <TextBlock Text="{x:Bind ViewModel.Phone, Mode=OneWay}" FontSize="16" Padding="5"/>
            </Border>

            <!-- Địa chỉ -->
            <TextBlock Text="Địa chỉ:" FontSize="20" FontWeight="Bold"/>
            <Border BorderBrush="Black" BorderThickness="1" Margin="0,10,0,10">
                <TextBlock Text="{x:Bind ViewModel.Address, Mode=OneWay}" FontSize="16" Padding="5"/>
            </Border>
        </StackPanel>
    </Grid>
</Page>
