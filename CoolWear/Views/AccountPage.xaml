<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CoolWear.Views.AccountPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoolWear.Views"
    xmlns:viewModels="using:CoolWear.ViewModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
            <TextBlock Text="Tên chủ cửa hàng:" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="1" Grid.Column="0" BorderBrush="Black" BorderThickness="1" Margin="10">
            <TextBlock Text="{x:Bind ViewModel.OwnerName, Mode=OneWay}" FontSize="16" Padding="5"/>
        </Border>

        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
            <TextBlock Text="Email:" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="3" Grid.Column="0" BorderBrush="Black" BorderThickness="1" Margin="10">
            <TextBlock Text="{x:Bind ViewModel.Email, Mode=OneWay}" FontSize="16" Padding="5"/>
        </Border>

        <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Horizontal">
            <TextBlock Text="Số điện thoại:" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="5" Grid.Column="0" BorderBrush="Black" BorderThickness="1" Margin="10">
            <TextBlock Text="{x:Bind ViewModel.Phone, Mode=OneWay}" FontSize="16" Padding="5"/>
        </Border>

        <StackPanel Grid.Row="6" Grid.Column="0" Orientation="Horizontal">
            <TextBlock Text="Địa chỉ:" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="7" Grid.Column="0" BorderBrush="Black" BorderThickness="1" Margin="10">
            <TextBlock Text="{x:Bind ViewModel.Address, Mode=OneWay}" FontSize="16" Padding="5"/>
        </Border>

        <Button Grid.Row="8" Grid.Column="0" Content="Thay đổi" 
                Background="#5FE2FF" BorderBrush="Transparent" BorderThickness="0" 
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Padding="10,5" FontSize="16" FontWeight="Bold"/>
    </Grid>
</Page>
